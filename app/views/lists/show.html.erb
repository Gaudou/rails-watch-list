<div class="container">
   <h1><i class="fas fa-video"></i> <%= @list.name %></h1>

  <div class="container d-flex m-5">

    <% @bookmarks.each do |bookmark| %>
    <% @movie = Movie.find(bookmark.movie_id) %>

    <div class="card rounded-lg m-5 mx-auto" style="width: 200px;">
     <img src="<%=@movie.poster_url%>" class="card-img-top" alt="...">
     <div class="card-body">
       <h5 class="card-title"><%= @movie.title %></h5>
       <p class="card-text"><strong>Description:</strong>  <%= @movie.overview %></p>
       <p class="card-text"><strong>Rating :</strong>  <%= @movie.rating %></p>
     </div>
   </div>
    <% end %>
  </div>

  <span> <%= link_to 'Back To Home', lists_path, class:'btn btn-secondary'%> </span>
  <span> <%= link_to 'Add a Movie', new_list_bookmark_path(@list), class:'btn btn-secondary'%> </span>
</div>

